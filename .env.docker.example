# Docker Compose build image overrides. Keep defaults for Docker Hub.
# If Docker Hub is slow, replace these with your own registry mirror images.
SERVER_NODE_IMAGE=node:20-bookworm-slim
WEB_NODE_IMAGE=node:20-alpine
WEB_NGINX_IMAGE=nginx:alpine
